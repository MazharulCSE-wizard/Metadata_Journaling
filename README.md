# Metadata_Journaling
In this project, we have a corrupted disk or img file . We check the consistency and then check if the meta data of that file is correct or not. After that if the file is corrupted and meta datas of that file are not correct, we fix the meta data using metadata journaling
